---
const currentYear = new Date().getFullYear();
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>VibeBox Vending | Luxury Vending Solutions</title>
    <meta
      name="description"
      content="VibeBox Vending provides premium, cashless vending machines for apartments, offices, and commercial buildings in DC & MD."
    />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      :root {
        --bg: #050509;
        --bg-alt: #0b0b11;
        --gold: #f6c453;
        --gold-soft: #f6d789;
        --text-main: #f7f7f7;
        --text-muted: #a3a3b3;
        --card: #11111a;
        --border-soft: rgba(246, 196, 83, 0.2);
      }

      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "SF Pro Text",
          sans-serif;
        background: radial-gradient(circle at top, #151520 0, #050509 55%);
        color: var(--text-main);
        min-height: 100vh;
      }

      a {
        color: inherit;
        text-decoration: none;
      }

      .page {
        max-width: 1100px;
        margin: 0 auto;
        padding: 32px 20px 56px;
      }

      /* NAV */
      .nav {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 32px;
      }

      .logo-wrap {
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .logo-box {
        width: 40px;
        height: 40px;
        border-radius: 11px;
        background: radial-gradient(circle at top left, #ffe8a6, #b68b28);
        display: flex;
        align-items: center;
        justify-content: center;
        color: #111;
        font-weight: 800;
        font-size: 18px;
        box-shadow: 0 0 18px rgba(246, 196, 83, 0.5);
      }

      .brand-text {
        display: flex;
        flex-direction: column;
        gap: 2px;
      }

      .brand-name {
        font-weight: 700;
        letter-spacing: 0.05em;
        font-size: 15px;
        text-transform: uppercase;
      }

      .brand-tag {
        font-size: 11px;
        color: var(--text-muted);
      }

      .nav-cta {
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: 12px;
      }

      .nav-pill {
        padding: 5px 11px;
        border-radius: 999px;
        border: 1px solid var(--border-soft);
        color: var(--gold-soft);
        background: rgba(10, 10, 15, 0.8);
      }

      /* HERO */

      .hero {
        display: grid;
        grid-template-columns: minmax(0, 3fr) minmax(0, 2.3fr);
        gap: 32px;
        align-items: center;
      }

      .hero-left {
        display: flex;
        flex-direction: column;
        gap: 18px;
      }

      .badge-row {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        font-size: 11px;
      }

      .badge {
        border-radius: 999px;
        border: 1px solid var(--border-soft);
        padding: 4px 11px;
        color: var(--gold-soft);
        background: rgba(15, 15, 22, 0.9);
      }

      .hero-title {
        font-size: clamp(30px, 5vw, 40px);
        font-weight: 750;
        line-height: 1.1;
      }

      .hero-title span {
        color: var(--gold);
      }

      .hero-sub {
        color: var(--text-muted);
        font-size: 14px;
        max-width: 460px;
      }

      .hero-cta-row {
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        align-items: center;
        margin-top: 6px;
      }

      .btn-primary {
        padding: 10px 20px;
        border-radius: 999px;
        border: none;
        background: linear-gradient(135deg, #f6c453, #e09c3d);
        color: #111;
        font-weight: 650;
        font-size: 14px;
        cursor: pointer;
        box-shadow: 0 12px 25px rgba(0, 0, 0, 0.55);
      }

      .btn-outline {
        padding: 9px 18px;
        border-radius: 999px;
        border: 1px solid var(--border-soft);
        background: rgba(13, 13, 20, 0.85);
        color: var(--text-main);
        font-size: 13px;
        cursor: pointer;
      }

      .cta-note {
        font-size: 11px;
        color: var(--text-muted);
      }

      /* HERO CARD (RIGHT) */

      .hero-card {
        border-radius: 18px;
        padding: 18px 18px 16px;
        background: radial-gradient(circle at top, #20202c 0, #101018 35%, #050509 100%);
        border: 1px solid rgba(255, 255, 255, 0.03);
        box-shadow: 0 18px 40px rgba(0, 0, 0, 0.7);
        position: relative;
        overflow: hidden;
      }

      .hero-card-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 10px;
        font-size: 12px;
      }

      .hero-card-title {
        font-weight: 600;
      }

      .hero-chip {
        padding: 4px 9px;
        border-radius: 999px;
        background: rgba(8, 8, 12, 0.7);
        border: 1px solid var(--border-soft);
        color: var(--gold-soft);
      }

      .machine-preview {
        border-radius: 14px;
        border: 1px solid var(--border-soft);
        background: linear-gradient(145deg, #111118, #050509);
        padding: 14px;
        margin-bottom: 10px;
      }

      .machine-body {
        display: grid;
        grid-template-columns: 1.3fr 1fr;
        gap: 12px;
        align-items: center;
      }

      .machine-visual {
        height: 160px;
        border-radius: 10px;
        background: linear-gradient(145deg, #f6c453, #111111);
        padding: 6px;
        display: grid;
        grid-template-rows: auto 1fr auto;
      }

      .machine-top {
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 9px;
        font-weight: 600;
        color: #111;
      }

      .machine-grid {
        margin-top: 4px;
        border-radius: 6px;
        background: #111111;
        padding: 4px;
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        grid-auto-rows: 15px;
        gap: 3px;
      }

      .slot {
        border-radius: 3px;
        background: linear-gradient(145deg, #222, #444);
      }

      .slot.highlight {
        background: linear-gradient(145deg, #f6c453, #e09c3d);
      }

      .machine-bottom {
        margin-top: 6px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 9px;
        color: #111;
      }

      .reader {
        padding: 3px 6px;
        border-radius: 999px;
        background: #111;
        color: var(--gold-soft);
        font-size: 9px;
      }

      .machine-meta {
        font-size: 11px;
        color: var(--text-muted);
        display: flex;
        flex-direction: column;
        gap: 6px;
      }

      .meta-row {
        display: flex;
        gap: 8px;
      }

      .meta-pill {
        padding: 4px 9px;
        border-radius: 999px;
        background: rgba(10, 10, 16, 0.9);
        border: 1px solid rgba(255, 255, 255, 0.04);
        font-size: 10px;
      }

      .hero-card-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 11px;
        color: var(--text-muted);
      }

      .hero-card-footer span strong {
        color: var(--gold-soft);
      }

      /* SECTIONS */

      .section {
        margin-top: 40px;
      }

      .section-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
        margin-bottom: 18px;
        gap: 12px;
      }

      .section-title {
        font-size: 18px;
        font-weight: 650;
      }

      .section-sub {
        font-size: 12px;
        color: var(--text-muted);
        max-width: 360px;
      }

      .pill-row {
        display: flex;
        flex-wrap: wrap;
        gap: 6px;
        font-size: 11px;
      }

      .pill {
        padding: 3px 9px;
        border-radius: 999px;
        border: 1px solid rgba(255, 255, 255, 0.08);
        color: var(--text-muted);
        background: rgba(5, 5, 10, 0.9);
      }

      .card-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 16px;
      }

      .card {
        border-radius: 14px;
        background: var(--card);
        border: 1px solid rgba(255, 255, 255, 0.03);
        padding: 14px 14px 13px;
      }

      .card-label {
        font-size: 11px;
        text-transform: uppercase;
        color: var(--gold-soft);
        letter-spacing: 0.12em;
        margin-bottom: 3px;
      }

      .card-title {
        font-size: 14px;
        font-weight: 600;
        margin-bottom: 6px;
      }

      .card-text {
        font-size: 12px;
        color: var(--text-muted);
      }

      .bullet-list {
        margin-top: 6px;
        font-size: 12px;
        color: var(--text-muted);
      }

      .bullet-list li {
        margin-left: 16px;
        margin-bottom: 3px;
      }

      /* PROCESS STEPS */

      .steps {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
        gap: 14px;
      }

      .step {
        display: flex;
        gap: 10px;
        align-items: flex-start;
      }

      .step-num {
        width: 22px;
        height: 22px;
        border-radius: 999px;
        border: 1px solid var(--border-soft);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 11px;
        color: var(--gold-soft);
        background: rgba(7, 7, 11, 0.95);
      }

      .step-body {
        font-size: 12px;
      }

      .step-title {
        font-weight: 600;
        margin-bottom: 3px;
      }

      .step-text {
        color: var(--text-muted);
      }

      /* CONTACT STRIP */

      .contact-strip {
        margin-top: 38px;
        border-radius: 16px;
        padding: 16px 16px 14px;
        background: linear-gradient(135deg, #f6c453, #111111);
        color: #111;
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
        gap: 14px;
        align-items: center;
      }

      .contact-main {
        font-size: 15px;
        font-weight: 650;
      }

      .contact-sub {
        font-size: 12px;
      }

      .contact-channels {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        font-size: 12px;
      }

      .chip-dark {
        padding: 6px 10px;
        border-radius: 999px;
        background: rgba(0, 0, 0, 0.82);
        color: #f6d789;
      }

      /* FOOTER */

      footer {
        margin-top: 28px;
        font-size: 11px;
        color: var(--text-muted);
        display: flex;
        justify-content: space-between;
        gap: 12px;
        flex-wrap: wrap;
      }

      @media (max-width: 800px) {
        .hero {
          grid-template-columns: minmax(0, 1fr);
        }

        .hero-card {
          order: -1;
        }

        .nav {
          flex-direction: row;
        }
      }

      @media (max-width: 600px) {
        .page {
          padding: 22px 16px 40px;
        }
        .hero-title {
          font-size: 27px;
        }
      }
    </style>
  </head>
  <body>
    <main class="page">
      <!-- NAV -->
      <header class="nav">
        <div class="logo-wrap">
          <div class="logo-box">VB</div>
          <div class="brand-text">
            <div class="brand-name">VibeBox Vending</div>
            <div class="brand-tag">Urban Vending Solutions · DC / MD</div>
          </div>
        </div>
        <div class="nav-cta">
          <div class="nav-pill">Cashless • 24/7 Support</div>
        </div>
      </header>

      <!-- HERO -->
      <section class="hero">
        <div class="hero-left">
          <div class="badge-row">
            <div class="badge">Apartments • Offices • Commercial</div>
            <div class="badge">No-cost placement for qualifying buildings</div>
          </div>
          <h1 class="hero-title">
            Luxury <span>vending</span> that matches your building’s energy.
          </h1>
          <p class="hero-sub">
            VibeBox Vending installs and manages premium, cashless vending
            machines tailored for modern DC &amp; MD properties. Clean look,
            reliable service, and products your residents actually eat.
          </p>

          <div class="hero-cta-row">
            <a href="mailto:vibeboxvending@gmail.com">
              <button class="btn-primary">
                Request placement proposal
              </button>
            </a>
            <a href="https://www.instagram.com/vibeboxvending" target="_blank">
              <button class="btn-outline">
                View Instagram portfolio
              </button>
            </a>
          </div>
          <div class="cta-note">
            Response window: same day for DC / MD inquiries.
          </div>
        </div>

        <!-- RIGHT HERO CARD -->
        <aside class="hero-card">
          <div class="hero-card-header">
            <div class="hero-card-title">Signature Gold Series</div>
            <div class="hero-chip">Black &amp; Gold Finish</div>
          </div>
          <div class="machine-preview">
            <div class="machine-body">
              <div class="machine-visual">
                <div class="machine-top">
                  <span>VIBEBOX</span>
                  <span>DC · MD</span>
                </div>
                <div class="machine-grid">
                  <div class="slot highlight"></div>
                  <div class="slot"></div>
                  <div class="slot"></div>
                  <div class="slot"></div>
                  <div class="slot"></div>
                  <div class="slot highlight"></div>
                  <div class="slot"></div>
                  <div class="slot"></div>
                  <div class="slot"></div>
                  <div class="slot"></div>
                  <div class="slot"></div>
                  <div class="slot highlight"></div>
                </div>
                <div class="machine-bottom">
                  <span>Snacks • Drinks • Essentials</span>
                  <span class="reader">Tap • Chip • Mobile</span>
                </div>
              </div>
              <div class="machine-meta">
                <div>
                  High-end machines wrapped in a black and gold theme to match
                  luxury lobbies and modern common areas.
                </div>
                <div class="meta-row">
                  <div class="meta-pill">Cashless only</div>
                  <div class="meta-pill">Remote monitoring</div>
                </div>
                <div class="meta-row">
                  <div class="meta-pill">Curated product mix</div>
                  <div class="meta-pill">Service included</div>
                </div>
              </div>
            </div>
          </div>
          <div class="hero-card-footer">
            <span>
              Avg. building size: <strong>50+ residents</strong>
            </span>
            <span>
              Placement fee: <strong>$0 for qualifying sites</strong>
            </span>
          </div>
        </aside>
      </section>

      <!-- WHY BUILDINGS LIKE YOU -->

      <section class="section">
        <div class="section-header">
          <div>
            <div class="section-title">Why property managers choose VibeBox</div>
            <div class="section-sub">
              We keep it simple: clean machines, fast response times, and a
              product mix built around your residents, not just whatever’s cheap.
            </div>
          </div>
          <div class="pill-row">
            <div class="pill">Luxury &amp; mid-rise apartments</div>
            <div class="pill">Corporate offices</div>
            <div class="pill">Closed-access buildings</div>
          </div>
        </div>

        <div class="card-grid">
          <div class="card">
            <div class="card-label">01</div>
            <div class="card-title">No-cost placement</div>
            <div class="card-text">
              For qualifying buildings, we cover the machine, install, and
              maintenance. You provide the space and power, we handle the rest.
            </div>
          </div>

          <div class="card">
            <div class="card-label">02</div>
            <div class="card-title">Modern, cashless experience</div>
            <div class="card-text">
              Tap-to-pay, Apple Pay, Google Pay, and chip/card payments. No jammed
              bill acceptors or coin headaches.
            </div>
          </div>

          <div class="card">
            <div class="card-label">03</div>
            <div class="card-title">Curated for your crowd</div>
            <ul class="bullet-list">
              <li>Classic snacks &amp; drinks</li>
              <li>Health-conscious options on request</li>
              <li>Late-night essentials for residents &amp; staff</li>
            </ul>
          </div>
        </div>
      </section>

      <!-- SERVICES / PACKAGES -->

      <section class="section">
        <div class="section-header">
          <div>
            <div class="section-title">Placement options</div>
            <div class="section-sub">
              Whether you want one machine in the lobby or a full vending lineup,
              we’ll design a setup that makes sense for your building.
            </div>
          </div>
        </div>

        <div class="card-grid">
          <div class="card">
            <div class="card-label">Core Package</div>
            <div class="card-title">Standard Snack &amp; Drink</div>
            <div class="card-text">
              Our most popular setup: one combo snack/drink machine with
              high-turn snacks, cold drinks, and resident favorites.
            </div>
            <ul class="bullet-list">
              <li>Cashless-only payments</li>
              <li>Full service &amp; restocking included</li>
              <li>Ideal for 40–150 unit buildings</li>
            </ul>
          </div>

          <div class="card">
            <div class="card-label">Premium</div>
            <div class="card-title">Black &amp; Gold Lobby Series</div>
            <div class="card-text">
              Custom-wrapped machines in a black &amp; gold theme to match
              luxury finishes and modern interiors.
            </div>
            <ul class="bullet-list">
              <li>Brand-aligned machine aesthetic</li>
              <li>Tailored product mix for your demographic</li>
              <li>Perfect for high-end residential &amp; offices</li>
            </ul>
          </div>

          <div class="card">
            <div class="card-label">Add-ons</div>
            <div class="card-title">Custom solutions</div>
            <div class="card-text">
              Need something different? We can discuss:
            </div>
            <ul class="bullet-list">
              <li>Multiple machines per property</li>
              <li>Employee-only or staff breakroom access</li>
              <li>Specialty products for your building</li>
            </ul>
          </div>
        </div>
      </section>

      <!-- PROCESS -->

      <section class="section">
        <div class="section-header">
          <div>
            <div class="section-title">How placement works</div>
            <div class="section-sub">
              Quick, low-lift process for property managers. We stay responsive so
              you don’t have to chase anyone down.
            </div>
          </div>
        </div>

        <div class="steps">
          <div class="step">
            <div class="step-num">1</div>
            <div class="step-body">
              <div class="step-title">Intro call or email</div>
              <div class="step-text">
                Share your building type, location, and approximate resident/employee
                count. We confirm if you qualify for no-cost placement.
              </div>
            </div>
          </div>

          <div class="step">
            <div class="step-num">2</div>
            <div class="step-body">
              <div class="step-title">Site check</div>
              <div class="step-text">
                We confirm power, access, and best placement spot (lobby, mailroom,
                amenity level, etc.).
              </div>
            </div>
          </div>

          <div class="step">
            <div class="step-num">3</div>
            <div class="step-body">
              <div class="step-title">Agreement &amp; install date</div>
              <div class="step-text">
                Simple placement agreement, then we schedule delivery and install
                around your building’s quiet hours and access rules.
              </div>
            </div>
          </div>

          <div class="step">
            <div class="step-num">4</div>
            <div class="step-body">
              <div class="step-title">Restocking &amp; upkeep</div>
              <div class="step-text">
                We monitor inventory, handle restocking, and respond quickly to any
                service issues. Your team never has to touch the machine.
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- CONTACT STRIP -->

      <section class="contact-strip">
        <div>
          <div class="contact-main">
            Ready to add a VibeBox to your building?
          </div>
          <div class="contact-sub">
            Send your building name, address, and a contact number and we’ll follow
            up with a placement proposal.
          </div>
        </div>
        <div class="contact-channels">
          <div class="chip-dark">
            Email: <strong>vibeboxvending@gmail.com</strong>
          </div>
          <div class="chip-dark">
            Instagram: <strong>@vibeboxvending</strong>
          </div>
          <div class="chip-dark">
            Service area: <strong>Washington, DC &amp; Maryland</strong>
          </div>
        </div>
      </section>

      <footer>
        <span>© {currentYear} VibeBox Vending LLC. All rights reserved.</span>
        <span>Urban Vending Solutions · DC / MD</span>
      </footer>
    </main>
  </body>
</html>
